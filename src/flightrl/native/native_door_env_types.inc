typedef struct {
    float score;
    float episode_return;
    float episode_length;
    float success_rate;
    float collision_rate;
    float outside_fov_success_fraction;
    float outside_fov_episode_fraction;
    float outside_fov_observed_fraction;
    float observed_episode_fraction;
    float door_visible_fraction;
    FlightRLDoorGroupLog groups;
    float n;
} Log;

typedef struct {
    Log log;
    float *observations;
    float *actions;
    float *rewards;
    float *terminals;
    int num_agents;
    uint32_t rng;
    uint32_t appearance_rng;
    FlightRLDoorEpisodeRng episode_rng;
    float control_dt;
    float maximum_evidence_age_s;
    int physics_substeps;
    int max_episode_steps;
    FlightRLDoorMissionConfig mission;
    FlightRLDoorMissionState mission_state;
    float max_horizontal_speed;
    float max_vertical_speed;
    float max_yawrate_deg_s;
    float velocity_gain;
    float attitude_gain;
    float vertical_gain;
    float camera_mean_min;
    float camera_mean_max;
    float domain_randomization;
    float layout_diversity;
    float camera_randomization;
    float obstacle_probability;
    float camera_mask;
    float base_room[7];
    float room[7];
    float base_physics[SIXDOF_PHYSICS_DIM];
    float physics[SIXDOF_PHYSICS_DIM];
    float position[3];
    float velocity[3];
    float quaternion[4];
    float body_rates[3];
    float ranges[6];
    float thrust_state;
    float low_level_action[4];
    float state_observation[28];
    float previous_action[FLIGHTRL_DOOR_ACTION_DIM];
    float origin_position[3];
    float origin_yaw;
    float takeoff_origin_z;
    uint8_t previous_frame[SIXDOF_VISION_PIXELS];
    FlightRLDoorScene scene;
    FlightRLDoorDetector detector;
    uint8_t layout_family;
    uint8_t scene_group_id;
    float camera_mean;
    int scene_seed;
    int control_step;
    int visible_steps;
    unsigned char terminal;
    unsigned char truncation;
    float current_return;
    float current_length;
} FlightRLDoorEnv;
